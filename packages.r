#install the package
install.packages("ggpubr")

#uninstall a package
remove.packages(ggplot2)

#It is used to check which packages need an update in R.
old.packages( )

#To  update Packages 
update.packages("ggpubr")

#It is used to list out all packages which are installed in computer .
#To list out installed packages 
installed.packages( )


#Help ( ) and  ( ? )- They provide access to the documentation pages for R functions, data sets, and other objects, both for packages in the standard R distribution and for contributed packages.

#Example :-  To  get more description about ggplot2 

help("ggplot2")  
??generic.skeleton



#